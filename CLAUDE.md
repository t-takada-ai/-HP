# X運用自動化システム 運用ルール

このリポジトリはX投稿を半自動運用するための基盤。投稿作成に関わるすべてのセッションで以下の鉄則を厳守すること。

## 鉄則(すべての投稿作成で厳守)

1. **ネタはIDで選ぶ**
   投稿ネタは必ず各スキルのネタ帳(`ネタ帳.md` / `idea_bank.md`)の「選べるネタ本体」からIDで選定する。
   その場の思いつきでネタを作らない。新ネタの提案はよいが、採用する場合はまずネタ帳にID登録してから使う。

2. **使ったら即台帳更新**
   投稿を確定・入稿したら、同一ターン内で使用済み台帳に1行追記する。
   台帳更新が済んでいない投稿を「完了」と報告しない。

3. **使用済みIDは二度と使わない**
   再放送は原則禁止。使用済み表にあるIDは選定候補から除外する。

4. **重複チェックは機械的に行う**
   採用候補が決まったら、題材のキーワード(類義語含む)で
   `materials/past_posts_index.md` と各スキルの `knowledge/past_*.md` をgrepし、
   既存投稿と題材・切り口が被っていないか確認する。近い投稿があれば別IDに差し替えるか、
   切り口の差別化ポイントを明記した上で採用する。

5. **1投稿=1サイクル**
   選定 → 4条件チェック → 重複チェック → 生成 → 入稿ファイル保存 → 台帳更新 → 投稿ログ追記。
   ここまで終えて1件完了。途中で止める場合は、どこまで進んだかを台帳側に明記する。

## アカウントと投稿タイプの対応

| 投稿タイプ | スキル | アカウント | 台帳 |
|---|---|---|---|
| ツリー型 | x-thread-post-writing | (TODO: @アカウント名) | ネタ帳.md ①使用済みネタ記録 |
| ストーリー型 | x-story-post-writing | (TODO: @アカウント名) | idea_bank.md 使用済み節 |

※ アカウントを分ける場合(メイン/サブ)はここに対応を追記し、混在させない。

## 台帳の一元化ルール

- 投稿タイプごとに台帳は1ファイルに集約する(ツリー型=`ネタ帳.md`内の使用済み表、ストーリー型=`idea_bank.md`)。
- `materials/past_posts_index.md` は全タイプ横断の索引。月1回、各スキルの投稿ログから再生成する
  (手動追記で二重管理しない。生成元は常に各スキルの `past_*.md`)。

## ネタ帳の改訂ルール

- ネタ帳・idea_bank はバージョン管理する(v1, v2, …)。改訂時はヘッダの版数・作成日・分析対象を更新。
- 改訂の入力は `materials/x_analysis_report.md`。感覚ではなくデータで改訂する。
- 旧版の使用済み記録は新版に必ず引き継ぐ(削除禁止。かぶり防止の履歴なので)。
